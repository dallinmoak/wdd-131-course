const headerMU = (toRoot = '.') => `

  <header>
    <a href="${toRoot}/index.html" class="logo">
      <img src="${toRoot}/logo.png" alt="Dallin Social Logo" />
    </a>
    <nav>
      <ul>
        <li><a href="#">Profile</a></li>
      </ul>
    </nav>
  </header>
  `;

export { headerMU };

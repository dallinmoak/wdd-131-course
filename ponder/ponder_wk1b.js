const PI = 3.14;
let radius = 3;

let area = PI * Math.pow(radius,2);
console.log(area);

radius = 20;

let newArea = PI * Math.pow(radius, 2);
console.log(newArea);

const one = 1;
const two = '2';

console.log(two);
console.log(one + parseInt(two));

let course = "CSE 131";

if (true) {
    let student = "John";
    console.log(course);
    console.log(student);
}

console.log(course);
console.log(student);